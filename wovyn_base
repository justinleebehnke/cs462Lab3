ruleset wovyn_base {
  meta {
    
  }
  
  global {
    
  }
  
  rule process_heartbeat {
    select when wovyn heartbeat
    pre {
      never_used = event:attrs.klog("attrs")
    }
    send_directive("Heartbeat Event Occurred")
  }
}
